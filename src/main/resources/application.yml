spring:
  application:
    name: authentication-service

server:
  port: ${PORT:8080}

app:
  jwt:
    secret: ${APP_JWT_SECRET:authentication-service-demo-secret-key-change-in-production-1234567890}
    expiration-minutes: 60