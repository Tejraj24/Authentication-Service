spring:
  application:
    name: authentication-service

server:
  port: 8080

app:
  jwt:
    secret: authentication-service-demo-secret-key-change-in-production-1234567890
    expiration-minutes: 60